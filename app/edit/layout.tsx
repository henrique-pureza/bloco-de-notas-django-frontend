import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Editar nota | Bloco de Notas"
};

export default function EditLayout({ children }: { children: React.ReactNode }) {
    return children;
}
